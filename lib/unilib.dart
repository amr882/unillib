import 'package:flutter/material.dart';

class UniLib extends StatelessWidget {
  const UniLib({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(debugShowCheckedModeBanner: false, home: Scaffold());
  }
}
